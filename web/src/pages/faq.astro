---
import BaseLayout from "../layouts/BaseLayout.astro";
import { faqPage } from "../lib/seo";
import { faqCategories as categories, faqItems, faqSubtitle, faqDescription } from "../lib/faq-data";

// Native <details>/<summary> instead of a React accordion: the answers stay in
// the DOM (crawlable for the FAQPage structured data) with no JS. The Q&A data
// itself lives once in ../lib/faq-data and is shared with the /faq.md and
// /faq.txt alt-format generator.
const title = "FAQ - gpui-query";
const description = faqDescription;
const jsonLd = faqPage({
  questions: faqItems.map((item) => ({ question: item.question, answer: item.answer })),
});
const ogImage = "/og/faq.png";
---
<BaseLayout title={title} description={description} canonicalPath="/faq" image={ogImage} jsonLd={jsonLd}>
  <div class="mx-auto max-w-7xl px-4 py-8 sm:px-6 sm:py-16 lg:px-8">
    <div class="mx-auto max-w-7xl">
      <div class="border-l-4 border-primary pl-5">
        <h1 class="text-3xl font-bold tracking-tight sm:text-4xl">Frequently Asked Questions</h1>
        <p class="mt-2 text-lg text-muted-foreground">
          {faqSubtitle}
        </p>
      </div>

      <div class="mt-14 space-y-14">
        {categories.map((category) => (
          <section>
            <div class="flex items-center gap-3 border-l-4 border-primary pl-4">
              <div class="flex h-8 w-8 shrink-0 items-center justify-center rounded-lg bg-primary/10 text-primary">
                <svg
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="2"
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  class="h-4 w-4"
                  set:html={category.icon}
                />
              </div>
              <h2 class="text-xl font-semibold tracking-tight sm:text-2xl">{category.label}</h2>
            </div>

            <div class="mt-6 divide-y divide-border/60">
              {category.items.map((item) => (
                <details class="group py-1">
                  <summary
                    class="flex cursor-pointer list-none items-center justify-between gap-4 py-4 text-left text-[15px] font-medium text-foreground/90 transition-colors duration-200 hover:text-primary group-open:text-primary"
                  >
                    <span>{item.question}</span>
                    <svg
                      class="h-4 w-4 shrink-0 text-muted-foreground transition-transform duration-200 group-open:rotate-180"
                      viewBox="0 0 24 24"
                      fill="none"
                      stroke="currentColor"
                      stroke-width="2"
                      stroke-linecap="round"
                      stroke-linejoin="round"
                      aria-hidden="true"
                    >
                      <path d="m6 9 6 6 6-6"></path>
                    </svg>
                  </summary>
                  <p class="pb-4 pr-8 leading-relaxed text-muted-foreground">{item.answer}</p>
                </details>
              ))}
            </div>
          </section>
        ))}
      </div>

      <div
        class="mt-20 rounded-2xl bg-gradient-to-br from-primary/10 via-primary/5 to-emerald-500/10 px-8 py-14 text-center sm:px-16"
      >
        <h2 class="text-2xl font-bold tracking-tight sm:text-3xl">Still have questions?</h2>
        <p class="mx-auto mt-3 max-w-md text-muted-foreground">
          Open an issue on GitHub and we will get back to you. Contributions and discussions are
          always welcome.
        </p>
        <div class="mt-8 flex flex-col items-center justify-center gap-4 sm:flex-row">
          <a
            href="https://github.com/freeoxide/gpui-query/issues"
            target="_blank"
            rel="noopener noreferrer"
            class="inline-flex h-11 items-center justify-center gap-2 bg-primary px-6 text-base font-semibold text-primary-foreground shadow-lg shadow-primary/20 transition-colors hover:bg-primary/90"
          >
            <svg viewBox="0 0 24 24" fill="currentColor" class="h-4 w-4" aria-hidden="true">
              <path
                d="M12 .5C5.7.5.5 5.7.5 12c0 5.1 3.3 9.4 7.9 10.9.6.1.8-.2.8-.5v-2c-3.2.7-3.9-1.4-3.9-1.4-.5-1.3-1.3-1.7-1.3-1.7-1.1-.7.1-.7.1-.7 1.2.1 1.8 1.2 1.8 1.2 1 1.8 2.8 1.3 3.5 1 .1-.8.4-1.3.7-1.6-2.6-.3-5.3-1.3-5.3-5.7 0-1.3.5-2.3 1.2-3.1-.1-.3-.5-1.5.1-3.1 0 0 1-.3 3.3 1.2a11.5 11.5 0 0 1 6 0C17 4.6 18 4.9 18 4.9c.6 1.6.2 2.8.1 3.1.8.8 1.2 1.8 1.2 3.1 0 4.4-2.7 5.4-5.3 5.7.4.4.8 1.1.8 2.2v3.3c0 .3.2.6.8.5 4.6-1.5 7.9-5.8 7.9-10.9C23.5 5.7 18.3.5 12 .5z"
              ></path>
            </svg>
            Open an Issue
            <svg
              class="ml-1 h-4 w-4"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
              aria-hidden="true"
            >
              <path d="M5 12h14"></path>
              <path d="m12 5 7 7-7 7"></path>
            </svg>
          </a>
          <a
            href="/docs/"
            class="inline-flex h-11 items-center justify-center gap-2 border border-input bg-background px-6 text-base font-semibold transition-colors hover:bg-accent hover:text-accent-foreground"
          >
            Read the Docs
            <svg
              class="ml-1 h-4 w-4"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
              aria-hidden="true"
            >
              <path d="M5 12h14"></path>
              <path d="m12 5 7 7-7 7"></path>
            </svg>
          </a>
        </div>
      </div>
    </div>
  </div>
</BaseLayout>
